local theme = {
    brandName = "Sora's Magical OS",
    subtitle = "Jolly Roger Helm Console",
    accent = colors.purple,
    accentLight = colors.magenta,
    text = colors.white,
    muted = colors.lightGray,
    shadow = colors.black,
    warning = colors.red,
    ok = colors.lime,
    panel = colors.gray,
}

return theme
